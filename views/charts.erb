<div class="container">
  <% @funds.each do |fund| %>
    <h2><%= fund.name %> Fund</h2>
    <div class="chart-wrapper">
      <canvas class="chart" data-fund="<%= fund.name %>"></canvas>
    </div>
  <% end %>
</div>
